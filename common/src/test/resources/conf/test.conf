run {
  InputSource {
    connectionURL = "someConnectionURL"
    userName = "someUserName"
    password = "somePassword"
    inputPath = "someInputPath"
    inputFormat = "someinputFormat"
  }
  OutputSource {
    connectionURL = "someConnectionURL"
    userName = "someUserName"
    password = "somePassword"
    outputPath = "someoutputPath"
    outputFormat = "someoutPutFormat"
  }
}