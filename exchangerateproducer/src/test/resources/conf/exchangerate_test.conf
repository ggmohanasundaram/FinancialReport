run {
  InputSource {
    connectionURL = "someConnectionURL"
    userName = "someUserName"
    password = "somePassword"
    inputPath = "testResults/testInput/"
    inputFormat = "json"
  }
  OutputSource {
    connectionURL = "someConnectionURL"
    userName = "someUserName"
    password = "somePassword"
    outputPath = "testResults/testOutPut/"
    outputFormat = "orc"
  }
}